<h1><%= @list.name %></h1>

<ul>
  <% @todos.each do |todo| %>
    <li><%= link_to todo.item, todo %></li>
  <% end %>
</ul>

<%= link_to '<button type="button">Add new To-Do List item</button>'.html_safe, '/todos/new/:list_id' %>
