<h2><%= @list.name %></h2>

<ul>
  <% @todos.each do |todo| %>
    <li><%= link_to todo.item, todo %></li>
  <% end %>

</ul>

<%= link_to '<button type="button">Add New To-Do List item</button>'.html_safe, '/todos/new' %>
