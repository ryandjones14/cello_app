
<%= form_for @todo do |f| %>
  <%= f.label :item %><br>
  <%= f.text_field :item %><br>
  <%= f.hidden_field :list_id, :value => @list.id%><br>
  <%= f.submit "New To Do", :class => 'btn btn-primary btn-xs' %>
<% end %>
